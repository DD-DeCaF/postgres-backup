pgbkp:
  image: 'dddecaf/postgres-backup:master'
  environment:
    - BACKUP_DIR=/data
    - PGHOST=db
    - PGDATABASE=postgres
    - PGUSER=postgres
#    - PGPORT=5437
#    - PGPASSWORD=
  links:
    - 'service.stack:db'
  volumes:
    - '/host/path:/data'
